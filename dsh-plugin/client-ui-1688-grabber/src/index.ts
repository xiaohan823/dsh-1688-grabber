/** Host loader entry for the 1688-grabber browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the grabber panel. */
export function apply(): void {}
